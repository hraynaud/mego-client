// import { API_PREFIX } from '../models/constants';
// import { authService } from '../services/auth.service';

// export const apiUrl = (url: string) =>
//   authService.isLoggedIn() ? `${API_PREFIX}${url}` : url;
